- id: e01
  prompt: "what went wrong in the last 24h?"
  expect_commands: [rtfm, health, log, changes]
  expect_max_calls: 5
  expect_writes: false

- id: e02
  prompt: "which automation failed last night?"
  expect_commands: [rtfm, auto ls, trace show]
  expect_max_calls: 5

- id: e03
  prompt: "is sensor.wp_vl behaving normally?"
  expect_commands: [rtfm, ent hist, ent anomalies]
  expect_max_calls: 4

- id: e04
  prompt: "disable automation.climate_schedule"
  expect_commands: [rtfm, svc call]
  expect_writes: true
  expect_confirmation_asked: true

- id: e05
  prompt: "show me the daily report"
  expect_commands: [rtfm, health, issues, log, changes]
  expect_max_calls: 6

- id: e06
  prompt: "which entities belong to the heat pump?"
  expect_commands: [rtfm, ent ls, ent related]
  expect_max_calls: 4

- id: e07
  prompt: "give me a list of all labels"
  expect_commands: [rtfm, label ls]
  expect_max_calls: 2

- id: e08
  prompt: "build a dashboard 'energy' with the solar sensors"
  expect_commands: [rtfm]
  expect_writes: true
  expect_confirmation_asked: true
